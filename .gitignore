# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
.cursor/
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# impeccable design tooling (local only — never commit)
.claude/skills/impeccable/
.impeccable/

# local editor / agent settings (per-machine)
.claude/settings.local.json

# graphify knowledge graph (generated — rebuild with `graphify .`)
/graphify-out/